{
  "name": "smartcare-api-server",
  "version": "1.0.0",
  "description": "Fake API server for SmartCare frontend development",
  "main": "db.json",
  "scripts": {
    "start": "json-server-auth ./db.json --port $PORT --host 0.0.0.0 --routes routes.json"
  },
  "dependencies": {
    "json-server": "^0.17.4",
    "json-server-auth": "^2.1.0"
  }
}
